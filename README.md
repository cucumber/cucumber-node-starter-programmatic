# cucumber-node-starter-programmatic

Starter template for a [cucumber-node](https://github.com/cucumber/cucumber-node) project executing a run via the programmatic API.

## Usage

Work on your feature files and support code in the `features` directory, and then run:

```shell
node ./run.js
```